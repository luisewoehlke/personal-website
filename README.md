# Personal website

A simple static site. Open `index.html` in a browser, or serve the folder with any static file server.

## Local preview

```bash
npx --yes serve .
```

Then visit the URL printed in the terminal.
